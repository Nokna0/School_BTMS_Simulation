# 열역학 모델 정리 (THERMODYNAMICS)

세 시뮬레이터에서 사용된 모든 열역학적 요소를
**2015 개정 교육과정 「고급물리학」 열역학 단원**과 대응시켜 정리한 문서입니다.

- **1–4장** — 열기관 시뮬레이터(`heat_engine_collapse_simulator.html`,
  `btms_heat_engine_simulation.html`)의 모델
- **5장** — 급속 충전 시나리오 시뮬레이터(`fast_charging_simulation.html`)의 모델

> 표기: `T` = 섭씨온도(°C), `T[K]` = 절대온도(K), `t` = 시간(s)
> 열기관 모델 상수: `m·c = 36 kJ/K`, `T_주위 = 25 °C`

---

## 1. 교육과정 기반 물리 (정확히 준수하는 부분)

### 1.1 절대온도(켈빈 온도)

```
T[K] = T[°C] + 273.15
```

- 코드: `KELVIN = 273.15`, `calcEff()` 내부에서 사용
- 카르노 효율 계산은 반드시 절대온도로 해야 하므로 모든 효율 계산에 적용
- **교육과정 대응:** 절대온도와 켈빈 온도 눈금

### 1.2 비열 · 열용량과 열량 (Q = m·c·ΔT)

엔진 블록을 하나의 집중 열용량(lumped capacitance)으로 모델링합니다.

```
Q = m · c · ΔT
열용량  C = m · c = 36 000 J/K = 36 kJ/K     (≈ 72 kg 주철, c ≈ 500 J/(kg·K))

미분형:  m · c · (dT/dt) = Q_net
이산형:  ΔT = Q_net · Δt / (m · c)
```

- 코드: `THERMAL_MASS = 36000`, `dTdt = (qin - qout) / THERMAL_MASS`
- **교육과정 대응:** 열량, 비열, 열용량

### 1.3 열역학 제1법칙 (ΔU = Q − W)

```
ΔU = Q − W
```

본 집중정수 모델은 기체의 부피 변화에 의한 일을 따로 분리하지 않으므로
**W ≈ 0** 으로 두고, 엔진에 출입하는 알짜 열이 내부에너지(→ 온도)를 변화시킵니다.

```
ΔU = Q_in − Q_out = m · c · ΔT      (W ≈ 0)
```

- 코드: `dTdt = (qin - qout) / THERMAL_MASS`
- 헤더 부제에 명시: `열역학 1법칙(ΔU = Q, W≈0)`
- **교육과정 대응:** 열역학 제1법칙, 내부에너지

### 1.4 열의 이동 · 열평형 (자연 방열)

표면을 통한 자연 방열은 주위와의 온도차에 비례합니다(뉴턴 냉각).

```
Q_passive = h · (T − T_주위) ,   h ≈ 30 W/K
```

- 코드: `passive = Math.max(0, T - AMBIENT) * 30`
- 온도차가 0이면 방열 0 → 주위와 **열평형**
- 열은 항상 고온부(엔진) → 저온부(주위)로 이동
- **교육과정 대응:** 열의 이동(전도·대류·복사), 열평형

### 1.5 열기관 모델 (고열원 / 저열원)

| 요소 | 역할 | 코드 |
|---|---|---|
| 고열원 (T_H) | 연소가스·엔진 본체 | 엔진 온도 `S.T` |
| 흡수 열 Q_in | 연료 연소열(부하에 비례) | `calcQin()` |
| 저열원 (T_C) | 냉각계 + 주위 | `AMBIENT = 25 °C` |
| 방출 열 Q_out | 자연 방열 + 라디에이터 | `calcQout()` |

```
Q_in = Q_in,max · (엔진 부하)        Q_in,max = 60 kW
```

- **교육과정 대응:** 열기관, 고열원·저열원 사이의 열 흐름

### 1.6 열효율 — 카르노 효율 (열역학 제2법칙)

두 열원 사이에서 작동하는 열기관의 **이론적 최대 효율**:

```
η = 1 − T_C / T_H        (T 는 절대온도 K)

T_H = T_engine + 273.15      (고열원)
T_C = 25 + 273.15            (저열원, 주위)
```

- 코드: `calcEff(T)` → `eta = 1 - T_C / T_H`
- 실제 열기관의 효율은 항상 **η_real ≤ η_carnot**
- 일반 정의와의 관계:  `η = W / Q_H = 1 − Q_C / Q_H`
  본 시뮬레이터는 두 열원의 절대온도로부터 **이론 한계(카르노 효율)** 를 표시
- **교육과정 대응:** 카르노 기관, 카르노 효율, 열역학 제2법칙

> **제2법칙의 관찰 포인트**
> - 상온(온도차 0)에서는 `η = 0` → 일을 전혀 얻을 수 없음
> - 온도차(T_H − T_C)가 커질수록 `η` 증가
> - 효율은 100%에 결코 도달할 수 없음 (비가역성)

---

## 2. 공학적 시나리오 요소 (교육과정 공식 아님)

아래 항목들은 열역학 법칙을 **위반하지 않는** 모델링 가정으로,
"냉각 실패 → 엔진 붕괴" 시나리오를 만들기 위한 공학적 보정입니다.
모두 열의 출입(Q_in / Q_out) **비율만** 바꾸며 1·2법칙과 모순되지 않습니다.

### 2.1 서모스탯 제어 냉각

```
T ≤ 82 °C        : 자연 방열만 (라디에이터 닫힘)
82 < T < 95 °C   : f = (T − 82) / (95 − 82)        (비례 개방)
T ≥ 95 °C        : f = 1                            (완전 개방)

Q_active = Q_out,max · (냉각 가동률) · f            Q_out,max = 80 kW
Q_out    = Q_passive + Q_active
```

### 2.2 냉각수 비등에 의한 열전달 저하

```
T ≥ 107 °C :  boilFrac = min(1, (T − 107) / 63)
              Q_active  ×= (1 − 0.9 · boilFrac)
```

기포(증기)가 냉각수–금속 열전달을 최대 90%까지 방해한다고 가정.

### 2.3 윤활유 열화에 의한 마찰 발열 증가

```
T ≥ 130 °C :  Q_in ×= 1 + min(0.35, (T − 130) / 70 · 0.35)      (최대 +35%)
```

### 2.4 고장 임계온도 (시나리오 이벤트 — 물리식 아님)

| 온도 | 사건 |
|---|---|
| 107 °C | 냉각수 비등 시작 |
| 130 °C | 윤활유 열화 |
| 150 °C | 헤드 가스켓 파손 |
| 200 °C | 피스톤 고착(엔진 정지) |

마찰 부하 게이지 표시값: `T ≥ 130 → min(100, (T − 130) / 0.7) %`

### 2.5 수치 적분

- 방법: 명시적 오일러법(explicit Euler)
- 시간 간격: `Δt = 0.1 s` (`DT`)
- 온도 범위 제한: `25 °C ≤ T ≤ 320 °C`

---

## 3. 파라미터 표

| 기호 | 코드 상수 | 값 | 의미 |
|---|---|---|---|
| m·c | `THERMAL_MASS` | 36 000 J/K | 엔진 열용량 |
| Q_in,max | `MAX_QIN` | 60 000 W | 100% 부하 연소 발열 |
| Q_out,max | `MAX_QOUT` | 80 000 W | 100% 냉각 라디에이터 용량 |
| T_주위 | `AMBIENT` | 25 °C | 저열원·주위 온도 |
| Δt | `DT` | 0.1 s | 적분 시간 간격 |
| — | `THERMOSTAT_OPEN` | 82 °C | 서모스탯 개방 시작 |
| — | `THERMOSTAT_FULL` | 95 °C | 서모스탯 완전 개방 |
| 273.15 | `KELVIN` | 273.15 | °C → K 변환 |

---

## 4. 교육과정 매핑 요약

| 2015 개정 고급물리학 열역학 개념 | 시뮬레이터 구현 |
|---|---|
| 절대온도(켈빈) | `T[K] = T + 273.15` |
| 열량·비열·열용량 `Q = mcΔT` | 온도 ODE `mc·dT/dt = Q_net` |
| 열역학 제1법칙 `ΔU = Q − W` | `ΔU = Q_in − Q_out` (W ≈ 0) |
| 열의 이동·열평형 | 자연 방열 `Q ∝ (T − T_주위)` |
| 열기관(고열원·저열원) | Q_in 흡수 / Q_out 방출 모델 |
| 카르노 효율·제2법칙 | `η = 1 − T_C / T_H` (절대온도) |

> 핵심 열역학 관계식(열량 보존·제1법칙·카르노 효율)은 교육과정 공식을
> 그대로 사용하며, 비등·마찰·서모스탯 등은 법칙을 위반하지 않는
> 시나리오 보정 요소입니다.

---

# 5. 급속 충전 시나리오 (`fast_charging_simulation.html`)

급속 충전 중 배터리 셀의 발열을 **BTMS 적용 / 냉각 없음** 두 경우로 비교하는
시뮬레이터입니다. 발열원이 연소열이 아니라 **줄열(전기 → 열)** 인 점만 다르고,
온도 변화는 1–4장과 동일하게 **열량 보존·제1법칙** 으로 적분합니다.

> 모델 상수: `T_외부 = 입력값`, `T_목표(BTMS) = 30 °C`,
> `V_팩 = 400 V`(표시용), 적분 `Δt = 1 s`(명시적 오일러법)

### 5.1 입력값과 출력값

| 입력값 | 코드 | 범위 | 결정하는 물리량 |
|---|---|---|---|
| 충전 전류 `I` | `input.current` | 50–350 A | 줄 발열 `I²R` |
| 외부 온도 `T_외부` | `input.ambient` | −10–45 °C | 저열원·초기 셀 온도 |
| 충전 시간 | `input.durationMin` | 5–60 분 | 적분 구간 길이 |
| 배터리 용량 `Q_cap` | `input.capacity` | 20–100 kWh | 열용량 `m·c`, 내부저항 `R` |

| 출력값 | 정의 |
|---|---|
| 최고 온도 | `max(T)` (충전 구간) |
| 평균 온도 | `ΣT / N` (시간 평균) |
| 냉각 효율 | `η_cool = ∫Q_cool dt / ∫Q_gen dt` (누적) |
| 에너지 손실 | `E_loss = ∫I²R dt` (줄열 누적, kJ·%) |

### 5.2 줄의 법칙 — 전기에너지의 열 변환 (교육과정)

```
Q_gen = I² · R          (순간 발열, W)
E_loss = ∫ I² · R dt    (누적 손실 에너지, J)
```

- 코드: `jouleHeat(I, cap, T) = I*I*rInternal(cap, T)`
- 충전 전류가 내부저항을 통과하며 발생하는 열 = 배터리에 저장되지 못하고
  버려지는 에너지(에너지 손실)
- **교육과정 대응:** 전기에너지의 전환, 줄열(`Q = I²Rt`)

### 5.3 열용량과 열량 (Q = m·c·ΔT, 교육과정)

배터리 팩을 하나의 집중 열용량으로 모델링합니다(1.2절과 동일한 원리).

```
m   = 6 kg/kWh · Q_cap                 (팩 포함 환산 질량)
c   = 900 J/(kg·K)                     (셀 팩 평균 비열)
m·c = 5 400 · Q_cap  [J/K]             (60 kWh → 324 kJ/K)

m · c · (dT/dt) = Q_gen − Q_cool − Q_loss
```

- 코드: `thermalMass(cap) = MASS_PER_KWH*cap*C_SPEC`
- **교육과정 대응:** 비열, 열용량, 열량 보존, 열역학 제1법칙(W ≈ 0)

### 5.4 열의 이동 — 자연 방열·BTMS 냉각 (교육과정)

```
자연 방열(뉴턴 냉각):  Q_loss = h · (T − T_외부),   h = 25 W/K
BTMS 능동 냉각:        Q_cool = min( Q_cool,max ,  U · (T − T_목표) ),  T > T_목표
                       U = 600 W/K,  Q_cool,max = 4 500 W · (Q_cap / 60)
```

- 온도차가 0이면 방열 0 → 주위와 **열평형**, 열은 항상 고온 → 저온으로 이동
- "냉각 없는 배터리" 는 `Q_cool = 0` → 줄열이 그대로 축적
- **교육과정 대응:** 열의 이동(전도·대류), 열평형

### 5.5 공학적 시나리오 요소 (교육과정 공식 아님)

1–4장과 같은 취지의, 법칙을 위반하지 않는 모델링 가정입니다.

```
내부저항의 용량·온도 의존:
  R(Q_cap, T) = R₀ · (60 / Q_cap) · [ 1 + α·(T − 30) ]   (T > 30 °C)
  R₀ = 0.08 Ω,  α = 0.006 /°C
```

- 작은 팩일수록 병렬 셀이 적어 내부저항이 큼 → 같은 전류에서 더 빨리 가열
- 고온일수록 저항이 커지는 **양성 피드백**(열폭주 메커니즘의 단순화) —
  냉각이 없으면 `Q_gen` 이 점점 커져 발산
- 고장 임계온도(시나리오 이벤트, 물리식 아님):

| 온도 | 사건 |
|---|---|
| 45 °C | Li-ion 수명 열화 시작(경고) |
| 60 °C | 급속 열화·셀 손상(위험) |
| 80 °C | 열폭주 위험 → 충전 차단 |

- 충전률(SOC) 표시: `ΔSOC = (V_팩·I − Q_gen)·Δt / (Q_cap·3.6×10⁶)`
  (단자 공급 전력에서 줄열 손실을 뺀 값이 셀에 저장)

### 5.6 교육과정 매핑 요약 (급속 충전)

| 2015 개정 고급물리학 개념 | 시뮬레이터 구현 |
|---|---|
| 전기에너지의 전환·줄열 | `Q_gen = I²·R`, `E_loss = ∫I²R dt` |
| 비열·열용량·열량 `Q = mcΔT` | `m·c = 5400·Q_cap`, 온도 ODE |
| 열역학 제1법칙 `ΔU = Q − W` | `m·c·dT/dt = Q_gen − Q_cool − Q_loss` (W ≈ 0) |
| 열의 이동·열평형 | 뉴턴 냉각 `Q ∝ (T − T_외부)` |

> 줄열·열량 보존·제1법칙·뉴턴 냉각은 교육과정 공식을 그대로 사용하며,
> 내부저항의 용량·온도 의존과 임계온도 이벤트는 법칙을 위반하지 않는
> 시나리오 보정 요소입니다.
